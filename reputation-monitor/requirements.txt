# Python 3.10+
# Install:   python -m venv .venv && .\.venv\Scripts\activate && pip install -r requirements.txt
#            python -m uvicorn app.main:app --reload

# Web / API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Persistence
sqlalchemy>=2.0.0
alembic>=1.13.0
# Optional for Postgres deployment (not required for the default SQLite demo):
# psycopg2-binary>=2.9.9

# Config / HTTP
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0

# Scraping
beautifulsoup4>=4.12.0
feedparser>=6.0.0
lxml>=5.0.0

# Company name matching
rapidfuzz>=3.6.0

# LLM — the core of analysis (both providers supported, see LLM_PROVIDER in .env)
anthropic>=0.34.0
openai>=1.40.0
